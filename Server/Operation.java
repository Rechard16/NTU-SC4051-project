package Server;

/**
 * Operation enum is used to define the operation type.
 * It can be READ, WRITE, MONITOR, DELETE, MOVE, UPDATE, OVERWRITE.
 */
public enum Operation {
    READ, WRITE, MONITOR,DELETE,MOVE,UPDATE,OVERWRITE
}
